# Sentio — ML API runtime dependencies
# (training deps like pandas, datasets are NOT needed here)

# Core ML (needed to load and run the model)
scikit-learn>=1.4.0
numpy>=1.26.0

# Flask API
flask>=3.0.0
flask-cors>=4.0.0
gunicorn>=21.2.0